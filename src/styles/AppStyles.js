import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  leftArrowIcon: {
    width: 25,
    height: 25,
    marginLeft: 15,
    marginBottom: 60,
  },
  leftArrowIconWithoutMargin: {
    width: 25,
    height: 25,
    marginLeft: 15,
  },
});

export default styles;
